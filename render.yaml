services:
  - type: web
    name: ai-search-backend
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        scope: build
      - key: TAVILY_API_KEY
        scope: build
      - key: PORT
        value: "5000"
